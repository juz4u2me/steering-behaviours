import { Vector } from "@glazier/vector-js";

class Behaviour {

    constructor(host) {
        this.host = host;
        this.steering = new Vector(0.0, 0.0);
    }

    seek = (target) => {

    }

    update = () => {

    }
}

export default Behaviour;